{
  "name": "my-sass",
  "version": "1.0.0",
  "description": "编译sass所用",
  "scripts": {
    "dev": "sass --no-source-map src/:out-css/",
    "watch": "sass --no-source-map src/:out-css/ --watch",
    "build": "sass --no-source-map --style=compressed src/:out-css/"
  },
  "author": "MixJade",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.77.3"
  }
}
